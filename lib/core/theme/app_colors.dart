import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Dark
  static const darkSurface = Color(0xFF121212);
  static const darkBackground = Color(0xFF1A1A1A);
  static const darkCard = Color(0xFF242424);
  static const darkBorder = Color(0xFF2E2E2E);

  // Light
  static const lightBackground = Color(0xFFFAFAF9);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightCard = Color(0xFFF5F5F0);
  static const lightBorder = Color(0xFFE5E5E0);

  // Accent
  static const primary = Color(0xFFD4A373);
  static const primaryLight = Color(0xFFE6C9A8);
  static const primaryDark = Color(0xFFB8860B);

  // Text
  static const textDarkPrimary = Color(0xFFF5F5F0);
  static const textDarkSecondary = Color(0xFFA0A0A0);
  static const textDarkMuted = Color(0xFF666666);
  static const textLightPrimary = Color(0xFF1A1A1A);
  static const textLightSecondary = Color(0xFF555555);
  static const textLightMuted = Color(0xFF999999);

  // Status
  static const success = Color(0xFF22C55E);
  static const error = Color(0xFFEF4444);
  static const warning = Color(0xFFF59E0B);
}
